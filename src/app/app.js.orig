import page from "../page/page.js";

function app() {
	"use strict";
	const appElement = document.getElementById("app");
	if (appElement !== null) {
		const pageNode = new page.ComponentClass(appElement, page.args, {});
	}
}

export default app;
